import { createTheme } from "@mui/material";

export const modernTheme = createTheme({
  palette: {
    mode: undefined,
    background: {
      paper: "#ff0000",
    },
    text: {
      primary: "#1235e3",
    },
  },
});
